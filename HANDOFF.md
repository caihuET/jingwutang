# 精武堂项目交接说明

## 一、当前状态
- 代码已推送生产并完成部署：Docker 服务更新、nginx reload、Redis 已接入。
- 数据库迁移 `migrations/007_add_guild_applications.sql` 已执行，新增 `guild_applications` 表。
- 前端静态资源引用统一为 `common.js?v=7`，nginx 静态缓存改为 1 小时。

## 二、本阶段完成内容
1. 实时聊天与通知
   - WebSocket JWT 鉴权，并校验 Redis 单点登录会话。
   - Redis pub/sub 跨 worker 推送，Redis 不可用时降级为本地推送。
   - 心跳 ping/pong、指数退避重连。
   - 未读、已读回执、在线状态写入 Redis。
   - 私聊免选好友：组合时间线 + `/好友名 内容` + Ctrl/Command 点击名称快捷发起。
   - 好友申请通知：红点 + 右上角提示 + 好友页自动刷新。
   - 聊天窗最小化时私聊消息显示红点。
2. 好友系统
   - 在线状态按连接计数维护，好友页 30 秒轮询刷新。
3. 帮派系统
   - 入帮改为申请制：申请、帮主查看、同意/拒绝，通过后写入成员。
4. 排行系统
   - 等级榜、战力榜、跑马灯 Top10、我的排名；战力实时计算与角色页一致。
5. 装备系统
   - 统一 6 个槽位：武器、头盔、衣甲、腰带、靴子、项链。
   - 强化局部刷新并显示 +1/-1；出售按等级/品质/强化计价，紫装二次确认。
6. 技能系统
   - 被动技能名按门派标准名兜底，兼容历史乱码数据。
7. 登录与安全
   - 单点登录：登录写入 Redis 会话，旧端 60 秒内自动退出。
8. 移动端适配
   - 顶部 `☰` 导航抽屉；聊天窗默认收起、半屏显示；输入框 16px 防自动放大。

## 三、部署与运维要点
- 后端代码改动后：`docker compose restart app nginx`；镜像方式部署则 `docker compose up -d --build`。
- 宿主机 nginx 的 `default.conf` 已合并 `/game/jwt/` 与 `/surveillance/`，`/game/jwt/` 必须保留 Upgrade 头。
- Redis 承载未读、在线、单点登录；不可用时相关能力降级，不影响基础收发。
- 前端静态资源缓存为 1 小时，更新 JS 后需同步提升 `common.js?v=` 版本号。

## 四、验证结果
- `pytest`：105 passed，13 failed（本地无 MySQL，环境问题）。
- `tests/test_chat_ws.py` + `tests/test_imports.py`：15 passed。
- `node --check`、`py_compile` 均通过。
- 线上 `/game/jwt/api/v1/health` 返回 200，WebSocket 404 已消失。

## 五、已知问题与待办
- 排行实时计算战力，玩家量大后性能待优化，建议补 Redis 排序缓存。
- 竞技场系统尚未开发，排行页未包含竞技场榜。
- 移动端需真机回归：导航抽屉、聊天红点、输入框缩放、单点退出。
- Redis 故障时单点登录不踢旧端，属于预期降级。
- 个别旧浏览器缓存可能仍显示旧版本，硬刷新或提升版本号即可。

## 六、下一步动作
- 双端真机回归并观察线上日志。
- 排行接入 Redis 缓存与定时刷新。
- 按设计文档开发竞技场系统及对应排行。
